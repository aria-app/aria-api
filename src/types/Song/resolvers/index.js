const Mutation = require('./Mutation');
const Query = require('./Query');
const Song = require('./Song');
const SongsResponseItem = require('./SongsResponseItem');

module.exports = {
  Mutation,
  Query,
  Song,
  SongsResponseItem,
};
